package ObjectClass;

public interface InterfaceShapes {
	double pi= 22.0/7.0;
	
	public abstract void AreaOfSquare(int side);   // Regular to to write. In interface public,static,abstract keywords are default no need to write.
	
	void AreaOfCircle(int rad);
	
}
